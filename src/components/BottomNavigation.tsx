// This component is obsolete in React Native as navigation is handled by Tab Navigator in App.tsx
// Keeping it as a no-op for compatibility with web version

export default function BottomNavigation() {
  return null;
}
